listeners:
  unix:
    path: "__PROXY_SOCKET__"
    mode: 0600

upstream:
  url: "__DOCKER_HOST__"

namespace: "team-denied-generic"

audit:
  denied_response_mode: "generic"

logging:
  level: "info"
  format: "json"

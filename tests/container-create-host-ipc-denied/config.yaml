listeners:
  unix:
    path: "__PROXY_SOCKET__"
    mode: 0600

upstream:
  url: "__DOCKER_HOST__"

namespace: "team-host-ipc"

audit:
  namespaces:
    ipc_mode:
      deny_host: true

logging:
  level: "info"
  format: "json"

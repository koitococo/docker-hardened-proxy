[project]
name = "docker-hardened-proxy-integration-tests"
version = "0.1.0"
description = "Integration tests for docker-hardened-proxy"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.32.5",
    "requests-unixsocket>=0.4.1",
]

[tool.uv]
package = false

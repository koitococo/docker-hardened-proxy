listeners:
  unix:
    path: "__PROXY_SOCKET__"
    mode: 0600

upstream:
  url: "__DOCKER_HOST__"

namespace: "team-privileged"

audit:
  deny_privileged: true

logging:
  level: "info"
  format: "json"

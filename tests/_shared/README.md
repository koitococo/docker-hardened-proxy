# 共享测试支撑约定

本目录保存所有集成测试共享的约定、模板与辅助代码。

## 共享输入

- `DOCKER_HOST`: 上游 Docker daemon
- 代理监听地址：每个测试应使用独立端口或独立 Unix socket
- 临时工作目录：每个测试应使用独立临时目录存放运行文件
- 容器命名规则：建议包含测试名与随机后缀，避免冲突

## 设计目标

- 测试独立
- 失败时易定位
- 清理行为可重复执行
- 后续可复用统一辅助库与运行器
